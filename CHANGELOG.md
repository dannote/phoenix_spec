# Changelog

## 0.1.0-dev

### Added

- Ecto schema introspection — fields, types, associations
- Phoenix JSON view AST analysis with alias resolution
- Nested view detection via `*JSON.data/1` calls
- OpenAPI 3.1 schema generation from extracted types
- Route-to-path mapping from Phoenix.Router
- Type mapping for all standard Ecto types including enums
- Mix task: `mix phoenix_spec.gen`
- JSON and YAML output formats
